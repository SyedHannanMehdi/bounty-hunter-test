{
  "name": "bounty-hunter-test",
  "version": "1.0.0",
  "description": "Bounty hunter test repo",
  "main": "dist/currency.js",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node test/run.js",
    "pretest": ""
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
